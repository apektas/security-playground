spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/playbank
    username: db_user
    password: 123qwe
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          # needed to for jwt token - remove this one and observe the issue
          # http://localhost:8090/realms/playground/.well-known/openid-configuration
          jwk-set-uri: http://localhost:8090/realms/playground/protocol/openid-connect/certs
server:
  port: 9999
